SELECT developers.name FROM developer_projects
INNER JOIN developers ON developer_projects.Developers_id = developers.id
INNER JOIN projects ON developer_projects.Projects_id = projects.id
WHERE (projects.name = "secondproject")